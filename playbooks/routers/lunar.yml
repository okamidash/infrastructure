---

- name: "Deploy router (TESTING)"
  hosts: lunar
  gather_facts: no
  vars:
  roles:
#    - virt/deployment
#    - common/github_ssh_key
#    - network/vyos/static_ip
    - role: network/vyos
      state: lunar
    - role: network/vyos
      state: "dhcpd"

